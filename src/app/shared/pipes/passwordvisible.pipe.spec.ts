import { PasswordvisiblePipe } from './passwordvisible.pipe';

describe('PasswordvisiblePipe', () => {
  it('create an instance', () => {
    const pipe = new PasswordvisiblePipe();
    expect(pipe).toBeTruthy();
  });
});
