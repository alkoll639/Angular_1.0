import { VisiblePasswordPipe } from './visible-password.pipe';

describe('VisiblePasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new VisiblePasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
